# Calculator Project

## Environment

Python version: 3.12.14

pytest version: 8.4.2

## Setup

I created a Python virtual environment named `.venv` and activated it.

I copied `requirements.txt` and `pytest.ini` from the `provided` directory into the project root.

I installed the required dependencies using `requirements.txt`.

## Verification

The virtual environment is excluded from Git.

The project uses the supplied pytest configuration.